{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path') or d.get('tool_response',{}).get('filePath') or '')\" | { read -r f; [ -n \"$f\" ] && echo \"$f\" | grep -q '\\.py$' && /home/henri/.local/bin/ruff format \"$f\"; } 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "Formatting Python file..."
          }
        ]
      }
    ]
  }
}
