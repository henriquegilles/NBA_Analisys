# ── Ambientes Python ─────────────────────────────────────────────────────────
.venv/
dbt_venv/
__pycache__/
*.pyc

# ── dbt artifacts (gerados automaticamente, não versionar) ───────────────────
logs/
target/
dbt_packages/

# ── Dagster ───────────────────────────────────────────────────────────────────
.dagster/
dagster_home/
dagster_venv/

# ── Docker ────────────────────────────────────────────────────────────────────
.docker/

# ── Arquivos pessoais do Claude Code ─────────────────────────────────────────
CLAUDE.local.md

# ── Secrets / ambiente ───────────────────────────────────────────────────────
.env
.dbt/.user.yml
.dbt/profiles.yml

# ── Seeds CSV (gerados pelos scrapers — dados brutos, não versionar) ──────────
# team_info.csv é referência estática e DEVE ser versionado — exceção explícita.
seeds/*.csv
!seeds/team_info.csv

# ── Jupyter ──────────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── Instaladores e binários ──────────────────────────────────────────────────
*.deb
*.exe
*.dll
*.pyd

# ── Cache de ferramentas ─────────────────────────────────────────────────────
.ruff_cache/
.wdm/

# ── Arquivos de configuração de outros editores de IA (não versionar) ─────────
.cursorrules
.windsurfrules
.opencode.json
AGENTS.md
GEMINI.md

# ── Grafo de conhecimento do code-review-graph (gerado localmente) ────────────
.code-review-graph/
seeds/team_info.csv
